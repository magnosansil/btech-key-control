# App (queries) — no Neon use a URL com "-pooler"
DATABASE_URL="postgresql://keytrack:keytrack@localhost:5432/keytrack?schema=public"
# Migrations — no Neon use a URL direta (sem pooler); local pode ser igual à DATABASE_URL
DIRECT_URL="postgresql://keytrack:keytrack@localhost:5432/keytrack?schema=public"
LATE_TOLERANCE_MINUTES=15
NEXT_PUBLIC_APP_NAME="Chave Fácil"
